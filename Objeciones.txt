Crear un archivo para los paquetes usando pip3 install -r requirements.txt:
<nombre_paquete>~=<version_paquete>

Teniendo el archivo alumnado.json:
with open("alumnado.json", "r") as j:
   fichero = json.load(j)

Descargar el archivo json de una url:
import requests
url = "https://raw.githubusercontent.com/cesaralvrz/curso-python/master/alumnado.json"
r = requests.get(url)
fichero = r.json()